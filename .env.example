# Gemini API keys — get free keys from https://aistudio.google.com/apikey
# Separate keys per role avoid one role exhausting another's shared quota
# (Gemini free-tier limits are per-project-per-model, not per-key)
RESEARCHER_API_KEY=
WRITER_API_KEY=
CRITIC_API_KEY=

# Tavily Search API key — free key from https://tavily.com
TAVILY_API_KEY=